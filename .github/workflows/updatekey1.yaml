name: update-key

on:
  push:
    branches:
      - main

jobs:
  esegui-script:
    runs-on: ubuntu-latest

    steps:
      - name: Vedi file
        run: ls
      - name: Esegui script Python
        run: python script.py
